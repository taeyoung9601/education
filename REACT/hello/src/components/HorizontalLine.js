console.groupCollapsed('src/components/HorizontalLine.js'); console.groupEnd();

function HorizontalLine() {
    console.groupCollapsed('HorizontalLine() invoked.'); console.groupEnd();

    return <hr/>;
}

export default HorizontalLine;