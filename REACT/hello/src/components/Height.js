console.groupCollapsed('src/components/Height.js'); console.groupEnd();

function Height() {
    console.groupCollapsed('Height() invoked.'); console.groupEnd();

    const height = 20 + 37.2
    return <p>height : {height}</p>
}

export default Height;