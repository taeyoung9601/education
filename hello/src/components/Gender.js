console.groupCollapsed('src/components/Gender.js'); console.groupEnd();

function Gender() {
    console.groupCollapsed('Gender() invoked.'); console.groupEnd();

    let gender = '남자';
    return <p>Gender:{ gender }</p>
}

export default Gender;