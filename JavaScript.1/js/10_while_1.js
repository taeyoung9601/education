// var 순차번호 = 1;  // 1부터 10까지 변화하는 값
// for(var counter= 0 ;counter < 10; counter++){
//     console.log(순차번호);
//     ++순차번호;
// }

// var 순차번호 = 1;  // 1부터 10까지 변화하는 값
// var counter = 0;

// while(counter<10){
//     console.log(순차번호);
    
//     ++순차번호;
//     //알고리즘의 스텝끝------------------------

//     ++counter;
// }

var 순차번호 = 1;  // 1부터 10까지 변화하는 값
var counter = 0;

do { 
    console.log(순차번호);
    ++순차번호;
    
    ++counter;
} while(counter<=9);